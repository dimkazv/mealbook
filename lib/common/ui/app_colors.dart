import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFFAFCFD);
  static const primary = Color(0xFFFFFFFF);
  static const accent = Color(0xFF3ABD69);
  static const textFaded = Color(0xFFBDBDBD);
  static const white = Color(0xFFFFFFFF);
  static const textPrimary = Colors.black;
  static const bottomBarIconSelected = Colors.black;
  static const bottomBarIcon = Color(0xFFDADADA);
  static const bottomBarShadow = Colors.black;
}
